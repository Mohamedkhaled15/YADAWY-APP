name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.5'
      SERVERPOD_PASSWORD_database: 'Jq5r7O_F79KxCpwknDqCQppb-b3c9iIV'
      SERVERPOD_PASSWORD_redis: '2vHBGZdO9yws2OlCPy5iYiJAPCBU0Do-'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: yadawy_app_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: yadawy_app_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: yadawy_app_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: yadawy_app_server
        run: serverpod generate

      - name: Run tests
        working-directory: yadawy_app_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: yadawy_app_server
        run: docker compose down -v
